/ {
    chosen {
        zephyr,display = &nice_view;
    };
};

// Falls &nice_view nicht erkannt wird, versuche es über den Alias 
// (dieser wird erst später aufgelöst und ist weniger fehleranfällig)
&nice_view {
    transform-rotation = <1>; // 90 Grad
};
