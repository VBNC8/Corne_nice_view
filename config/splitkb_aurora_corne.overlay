&spi0 {
    status = "okay";
    cs-gpios = <&gpio0 6 GPIO_ACTIVE_HIGH>;
};



/ {
    chosen {
        zephyr,display = &nice_view;
    };
};

/* Falls das Label nice_view oben nicht geht, nehmen wir den Pfad */
&{/soc/spi@40003000/ls0xx@0} {
    transform-rotation = <1>;
};
