// Wir lassen die SPI-Definition weg, da das Shield das übernimmt.

/ {
    chosen {
        // Das Shield definiert das Display als "nice_view"
        zephyr,display = &nice_view;
    };
};

// Falls du das Display drehen willst, nutzen wir das Label des Shields
&nice_view {
    status = "okay";
    // Falls die Anzeige auf dem Kopf steht, hier mit <0>, <90>, <180>, <270> spielen
};
