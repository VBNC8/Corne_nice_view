name: Draw ZMK keymaps
on:
  workflow_dispatch:
  push:
    paths:
      - "config/*.keymap"
      - "config/*.dtsi"
      - ".github/workflows/draw-keymap.yml"
      - "config/keymap_drawer.config.yaml"

jobs:
  draw:
    uses: caksoylar/keymap-drawer/.github/workflows/draw-zmk.yml@main
    with:
      keymap_patterns: "config/*.keymap"
      output_folder: "keymap-drawer"
      config_path: "config/keymap_drawer.config.yaml"
      # Hier fügen wir die Skip-Anweisung für den Parser hinzu:
      parse_args: "--skip-keys 0 13 26 25 38" 
      commit_message: "docs: [keymap-drawer] Randspalten entfernt"
